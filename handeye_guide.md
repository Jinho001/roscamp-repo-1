# TASK-V02: Hand-Eye 캘리브레이션 (Eye-in-Hand) 상세 수행 가이드

## 1. 개요 및 필수 전제 조건
* **목적:** 로봇 팔 끝단(End-Effector)과 카메라 렌즈 간의 물리적 거리 및 각도 차이(`T_ee2cam`)를 계산합니다.
* **전제 조건:**
  * V01 단계 성공적 완료 (`camera_info.yaml` 정상 생성됨)
  * 제어 PC에서 `stream_sender.py` 정상 실행 중

## 2. 물리적 환경 세팅 (가장 중요!)
* **체커보드 바닥 고정:** 체커보드가 1mm라도 밀리면 캘리브레이션은 실패합니다. 반드시 **테이프 등을 이용해 작업대 바닥에 단단히 고정**하세요.
* 이 단계에서는 로봇 팔만 움직입니다.

## 3. 실행 명령어

로봇(젯코봇)이 제어 PC에 연결되어 있고, 메인 PC에서 캘리브레이션을 진행해야 하므로 **제어 PC에서 영상과 로봇 좌표를 모두 보내주어야** 합니다.

### [터미널 1] 제어 PC (젯코봇이 연결된 PC)
기존에 켜둔 `stream_sender.py` 외에 추가로 로봇 좌표 전송 서버를 실행합니다.
```bash
python3 src/devices/jetcobot/vision/robot_coords_server.py
```

### [터미널 2] 메인 PC
제어 PC의 IP 주소(예: `192.168.1.xxx`)를 `--robot-host` 뒤에 입력하여 실행합니다.
```bash
python3 -m src.devices.jetcobot.vision.handeye_calibration \
  --remote \
  --robot-host <제어_PC_IP주소> \
  --square-mm 19.0 \
  --board 8x5
```

## 4. 데이터(자세) 수집 전략 (15 ~ 25장 권장)
Hand-Eye 캘리브레이션의 핵심은 **"카메라가 체커보드를 바라보는 각도(회전)의 다양성"**입니다. 단순히 로봇을 상하좌우로만 움직이면 수학적 해(방정식)를 풀 수 없습니다.

### 📸 권장하는 자세 수집 시나리오
1. **정면/기본 자세 (3~4장):**
   * 카메라가 바닥을 수직으로 내려다보는 자세에서 높이(Z)와 위치(X, Y)를 조금씩 바꿔가며 찍습니다.
2. **Pitch(상하 기울임) 변화 (5~6장):**
   * 로봇 헤드를 앞쪽으로 15~30도 숙이고, 뒤쪽으로 15~30도 젖힌 상태로 찍습니다.
3. **Roll/Yaw(좌우 비틀기/기울임) 변화 (5~6장):**
   * 카메라를 좌/우로 15~30도 정도 갸우뚱하게 꺾은 자세에서 찍습니다.
   * 제자리에서 카메라의 방향만 좌우로 회전시켜서도 찍습니다.
4. **다양한 위치에서 비스듬히 보기 (4~5장):**
   * 체커보드의 정중앙이 아닌, 체커보드가 화면의 모서리 쪽에 치우치게 보이도록 자세를 잡습니다.

*💡 주의: 모든 자세에서 체커보드 전체가 화면 안에 온전히 들어와야 합니다.*

## 5. 단계별 조작 및 검증 절차
1. 로봇 팔의 관절을 움직여 원하는 자세를 만듭니다.
2. 로봇을 멈추고 **미세한 진동이 사라질 때까지 1~2초간 대기**합니다.
3. **`스페이스바`**를 눌러 데이터를 캡처합니다. (화면에 초록색 "BOARD DETECTED" 확인)
4. 1~3번 과정을 반복하여 **최소 15장 이상(권장 20장 이상)** 수집합니다.
5. **`c` 키**를 눌러 캘리브레이션을 실행합니다. (성공 시 터미널에 행렬값이 출력되고 `handeye_result.yaml`이 자동 저장됩니다.)
6. **`v` 키**를 눌러 **검증(Verify) 모드**에 진입합니다.
   * 검증 모드 진입 후, 로봇을 캘리브레이션 때와는 조금 다른 **새로운 3가지 자세**로 이동시킨 뒤 각각 스페이스바를 누릅니다.
   * 측정된 **재현 오차(RMS)가 5.0 mm 미만**으로 나오면 캘리브레이션이 성공적으로 완료된 것입니다! (통상 2~4mm 이내가 나오면 매우 훌륭합니다.)

## 6. 트러블슈팅
* **검증 오차가 크게 나올 때:** 수집된 자세 중 회전(각도) 변화가 부족했거나, 캡처할 때 로봇이 흔들렸거나, 혹은 체커보드가 미세하게 움직였을 가능성이 높습니다. `r` 키를 눌러 수집 데이터를 초기화한 뒤, 각도 변화를 더 과감하게 주어 다시 수집해 보세요.
