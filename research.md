# 체커보드 인식 실패 원인 분석 (Research)

## 1. 개요
현재 `camera_calibration.py` 실행 중 체커보드 패턴이 정상적으로 검출("DETECTED")되지 않고 계속 "NO BOARD" 상태에 머무는 문제에 대한 원인 분석.

## 2. 관련 코드 및 설정
* **`camera_calibration.py`**: 
  * `CHECKERBOARD = (9, 6)` : 내부 코너 수 (가로 9, 세로 6). 즉, 사각형의 갯수가 가로 10개, 세로 7개인 패턴을 기대하고 있음.
  * `cv2.findChessboardCorners` 호출 시 플래그로 `cv2.CALIB_CB_ADAPTIVE_THRESH + cv2.CALIB_CB_NORMALIZE_IMAGE` 사용. 
* **`stream_sender.py`**:
  * 해상도: 640x480
  * JPEG 품질: 80
* **실행 계획서 (`execution_test_plan.md`)**:
  * 9x6 내부 코너 (정사각형 19mm) 체커보드를 사용해야 함을 명시.

## 3. 잠재적 원인 추론
1. **체커보드 규격 불일치 (가장 높은 확률)**:
   * 실제로 인쇄한 체커보드의 "내부 코너" 개수가 코드 설정인 (9, 6)이 아닐 수 있습니다. OpenCV의 체커보드 인식은 사각형 칸 개수가 아니라 **칸과 칸이 만나는 내부 교차점(코너)의 개수**를 기준으로 합니다.
   * 예: 사각형이 가로 10칸, 세로 7칸이면 내부 코너는 (9, 6)이 됩니다. 만약 가지고 계신 체커보드가 가로 9칸, 세로 6칸이라면 내부 코너는 (8, 5)가 되어 코드 설정과 불일치하여 절대 인식되지 않습니다.
2. **해상도 및 화질 문제**:
   * `stream_sender.py`에서 640x480 해상도로 전송 중입니다. 카메라가 체커보드와 너무 멀거나, 모션 블러가 발생하면 화질 저하로 인식이 실패할 수 있습니다.
3. **패턴 주변 여백(Quiet Zone) 부족**:
   * 체커보드의 가장자리 주변에 충분한 흰색 여백이 없으면 배경과 구분하지 못해 인식이 실패할 수 있습니다.
4. **조명 문제**:
   * 빛 반사가 심하거나 너무 어두울 경우 검은 칸과 흰 칸의 대비가 떨어집니다.

## 4. 해결 방향
* 실제 인쇄된 체커보드의 칸 수 또는 코너 수를 확인하여 코드에 반영해야 합니다.
* 하드코딩된 체커보드 크기를 실행 시 인자로 변경할 수 있도록 유연화해야 합니다.
