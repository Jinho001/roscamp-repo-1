# pick_place_profiles.yaml
# Pick/Place 동작별 파라미터 프로파일
# observe_pose: [x mm, y mm, z mm, rx deg, ry deg, rz deg]
# fixed_coords: 비전 없이 티칭 좌표로 직접 이동 [x, y, z, rx, ry, rz]

# ── Pick 프로파일 ──────────────────────────────────────────────────────────────

tray:                                     # 핑키 적재함 상자 픽업
  observe_pose: [-62.4, -87.9, 314.2, -161.81, 0.94, -179.37]
  z_surface_mm: 95.0
  hsv_lower: [0, 0, 208]
  hsv_upper: [158, 30, 255]
  min_w: 110
  max_w: 200
  min_h: 110
  max_h: 200
  pick_offset_mm: [0.0, 0.0, 0.0]         # [x, y, z] 보정값 (mm)
  grasp_roll: -178.81
  grasp_pitch: 0.94
  grasp_yaw_offset: 0.0
  pick_z_offset_mm: 20.0

receiving_zone:                           # 회수존(입고존) 상자 픽업
  observe_pose: [96.1, -7.9, 292.8, -172.78, -0.16, -90.14]
  z_surface_mm: 50.0
  hsv_lower: [0, 0, 208]
  hsv_upper: [158, 30, 255]
  min_w: 90
  max_w: 220
  min_h: 80
  max_h: 220
  pick_offset_mm: [-17.6, -9.9, 0.0]      # [x, y, z] 보정값 (mm) — 실측 오차 역산
  grasp_roll: -179.98
  grasp_pitch: 5.01
  grasp_yaw_offset: -179.0
  pick_z_offset_mm: 20.0

# NOTE: warehouse_pick은 1차 구현 범위 외 (추후 추가)

# ── Place 프로파일 ─────────────────────────────────────────────────────────────
# NOTE: pinky_tray_place, warehouse_place는 1차 구현 범위 외 (추후 추가)
